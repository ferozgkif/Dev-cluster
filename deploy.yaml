apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-apache
  namespace: kube
spec:
  selector:
    matchLabels:
      app: apache
  replicas: 1
  template:
    metadata:
      labels:
        app: apache

    spec:
      containers:
      - name: apache
        image: ferozfrk/apache:18
        ports:
        - containerPort: 80
